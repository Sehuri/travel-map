{
  "name": "sehuri-travel-map",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/*.test.cjs",
    "test:browser": "node tests/site-browser.cjs && node tests/discover-browser.cjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
